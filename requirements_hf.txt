# Requirements for Hugging Face Spaces deployment
# This includes all dependencies needed for the web interface

# Core dependencies for profanity filtering
faster-whisper>=1.2.1  # Primary transcription engine (4-10x faster, CTranslate2 backend)
torch>=2.0.0
numpy>=1.24.0

# Web interface - Gradio 6.0.0+
gradio>=6.0.0
huggingface_hub>=0.20.0
spaces>=0.43.0  # Required for Gradio hot-reloading on Hugging Face Spaces

# Note: FFmpeg is pre-installed on Hugging Face Spaces

