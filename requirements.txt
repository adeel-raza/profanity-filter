# Core dependencies for profanity filtering
faster-whisper>=1.2.1  # Primary transcription engine (4-10x faster than OpenAI Whisper)
torch>=2.0.0
numpy>=1.24.0
yt-dlp>=2024.0.0  # For YouTube video downloads

# Note: FFmpeg must be installed on the system
# Install with: sudo apt install ffmpeg (Linux) or brew install ffmpeg (macOS)
