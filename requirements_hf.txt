# Requirements for Hugging Face Spaces deployment
# This includes all dependencies needed for the web interface

# Core dependencies for profanity filtering
openai-whisper>=20231117
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0

# Web interface - updated to latest version
# Gradio 6.0.0+ (latest version, security fixes)
gradio>=6.0.0
huggingface_hub>=0.20.0

# Note: FFmpeg must be installed on the system
# Hugging Face Spaces has FFmpeg pre-installed

